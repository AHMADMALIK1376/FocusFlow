// FocusFlow ships a single fixed brand palette (matching the app logo — see
// design/tokens.css for the actual values). There is no user-selectable
// color scheme; light/dark MODE is the only remaining user preference.
export const MODES = ['light', 'dark'];
